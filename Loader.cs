using System;
using Transport;

class Loader
{
    static SNote[] snote = new SNote[100];
    static int snCount = 0;
    static Value table;

    static void writeSNote()
    {
        table = Value.MakeTable();
        
    }
}
